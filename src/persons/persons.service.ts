import { Injectable } from '@nestjs/common';

@Injectable()
export class PersonsService {

    getAll(): Object[]{
        const links: Object[] = [{}]
        return links;
    }
    async register(){
        
    }
}
